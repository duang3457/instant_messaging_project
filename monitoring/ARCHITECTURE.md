# 聊天室监控系统 - 架构图

## 📊 完整监控架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        聊天室系统监控架构                              │
└─────────────────────────────────────────────────────────────────────┘

应用层（数据生产）:
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  Comet Service   │  │  Logic Service   │  │   Job Service    │
│  (WebSocket)     │  │  (HTTP API)      │  │  (Kafka Worker)  │
│                  │  │                  │  │                  │
│  Port: 8081      │  │  Port: 8090      │  │                  │
│  Metrics: 9091   │  │  Metrics: 9092   │  │  Metrics: 9093   │
│                  │  │                  │  │                  │
│  监控指标:        │  │  监控指标:        │  │  监控指标:        │
│  • WebSocket     │  │  • HTTP Requests │  │  • Kafka消费     │
│    连接数         │  │  • Kafka生产     │  │  • gRPC调用      │
│  • 消息推送       │  │  • 业务延迟      │  │  • 消费延迟      │
│  • Redis操作     │  │  • 错误率        │  │  • 推送成功率    │
│  • 请求延迟       │  │                  │  │                  │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                     │
         │  HTTP GET /metrics  │                     │
         └─────────────────────┼─────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       Prometheus (时序数据库)                         │
│                         Port: 9090                                   │
│                                                                       │
│  功能:                                                                │
│  • 每 15 秒抓取一次指标                                               │
│  • 存储时序数据（保留 30 天）                                         │
│  • 执行 PromQL 查询                                                  │
│  • 评估告警规则                                                       │
│                                                                       │
│  数据流:                                                              │
│  Scrape → Store → Query → Alert                                     │
└────────┬───────────────────────────┬─────────────────┬──────────────┘
         │                           │                 │
         │ PromQL 查询               │ 告警触发         │ 数据源
         ▼                           ▼                 ▼
┌────────────────────┐    ┌──────────────────┐  ┌─────────────────┐
│     Grafana        │    │  Alertmanager    │  │   Loki (日志)   │
│   (可视化面板)      │    │   (告警管理)      │  │   Port: 3100    │
│   Port: 3000       │    │   Port: 9093     │  │                 │
│                    │    │                  │  │  日志来源:       │
│  功能:              │    │  功能:            │  │  • Comet        │
│  • Dashboard       │    │  • 告警分组       │  │  • Logic        │
│  • Explore 查询    │    │  • 告警路由       │  │  • Job          │
│  • 日志查看         │    │  • 去重抑制       │  │                 │
│  • 用户管理         │    │  • 多渠道通知     │  │  日志采集器:     │
│                    │    │                  │  │  Promtail       │
│  预定义面板:        │    │  通知渠道:        │  └─────────────────┘
│  • QPS 监控        │    │  ✉️ 邮件         │
│  • 延迟分析         │    │  📱 钉钉         │
│  • 错误率           │    │  💬 企业微信     │
│  • 连接数           │    │  🔔 Webhook     │
│  • Kafka 流量      │    │                  │
└────────────────────┘    └──────────────────┘


数据流向:
═══════════════════════════════════════════════════════════════════════

1️⃣  指标采集流程:
   应用 → /metrics 端点 → Prometheus → Grafana
                                   ↓
                              Alertmanager → 钉钉/邮件

2️⃣  日志聚合流程:
   应用日志文件 → Promtail → Loki → Grafana Explore

3️⃣  告警流程:
   Prometheus 评估规则 → 触发告警 → Alertmanager
                                         ↓
                     分组/路由/抑制 → 发送通知


核心指标类型:
═══════════════════════════════════════════════════════════════════════

📊 Counter (计数器 - 只增不减):
   • http_requests_total          - 总请求数
   • errors_total                 - 总错误数
   • kafka_messages_produced      - Kafka 生产消息数
   • kafka_messages_consumed      - Kafka 消费消息数
   • grpc_calls_total             - gRPC 调用次数
   • websocket_messages_pushed    - WebSocket 推送次数
   • redis_operations_total       - Redis 操作次数

📈 Gauge (仪表 - 可增可减):
   • active_connections           - 当前活跃连接数

📉 Histogram (直方图 - 分桶统计):
   • http_request_duration_μs     - 请求延迟分布
     Buckets: 100μs, 500μs, 1ms, 5ms, 10ms, 50ms, 100ms, 500ms, 1s, 5s, 10s


告警规则分类:
═══════════════════════════════════════════════════════════════════════

🚨 服务可用性:
   • ServiceDown                  - 服务下线

⚡ QPS 异常:
   • HighQPS                      - QPS 过高 (>1000/s)
   • QPSSurge                     - QPS 突增 (>50%)

⏱️  延迟告警:
   • HighP99Latency              - P99 延迟 >1s
   • HighP95Latency              - P95 延迟 >500ms

❌ 错误率:
   • HighErrorRate               - 错误率 >5%
   • HighErrorCount              - 错误数 >10/min

🔌 连接数:
   • HighConnectionCount         - 连接数 >10000
   • ConnectionDrop              - 连接突降 >50%

📨 Kafka:
   • KafkaLag                    - 消息积压 >1000

🌐 WebSocket:
   • HighWebSocketPushFailure    - 推送成功率 <95%

🔧 gRPC/Redis:
   • HighGrpcFailureRate         - gRPC 失败率 >5%
   • HighRedisFailureRate        - Redis 失败率 >1%


部署架构 (Docker Compose):
═══════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────┐
│                    Docker Compose 网络: monitoring                   │
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ Prometheus   │  │ Alertmanager │  │   Grafana    │              │
│  │  :9090       │  │    :9093     │  │    :3000     │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                       │
│  ┌──────────────┐  ┌──────────────┐                                 │
│  │    Loki      │  │  Promtail    │                                 │
│  │   :3100      │  │              │                                 │
│  └──────────────┘  └──────────────┘                                 │
│                                                                       │
│  数据卷:                                                              │
│  • prometheus_data  - Prometheus 时序数据                            │
│  • grafana_data     - Grafana 配置和仪表板                           │
│  • loki_data        - Loki 日志数据                                 │
└─────────────────────────────────────────────────────────────────────┘
        ↑
        │ host.docker.internal (访问宿主机)
        │
        ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         宿主机服务                                    │
│                                                                       │
│  • Comet  :9091/metrics                                              │
│  • Logic  :9092/metrics                                              │
│  • Job    :9093/metrics                                              │
└─────────────────────────────────────────────────────────────────────┘


文件结构:
═══════════════════════════════════════════════════════════════════════

monitoring/
├── docker-compose.yml               # Docker Compose 编排
├── prometheus.yml                   # Prometheus 配置
├── alert_rules.yml                  # 告警规则
├── alertmanager.yml                 # 告警管理配置
├── loki-config.yml                  # Loki 配置
├── promtail-config.yml              # Promtail 配置
├── start.sh                         # 启动脚本
├── test.sh                          # 测试脚本
├── README.md                        # 使用说明
├── IMPLEMENTATION_GUIDE.md          # 实施指南
├── DELIVERY.md                      # 交付文档
└── grafana/
    ├── provisioning/
    │   ├── datasources/
    │   │   └── datasources.yml      # 自动配置数据源
    │   └── dashboards/
    │       └── dashboards.yml       # 自动加载仪表板
    └── dashboards/
        └── chatroom-overview.json   # 预定义 Dashboard

server/application/chat-room/
└── monitoring/
    ├── metrics_collector.h          # 监控类头文件
    └── metrics_collector.cc         # 监控类实现


快速命令:
═══════════════════════════════════════════════════════════════════════

# 启动监控系统
cd /home/yang/chatroom/monitoring && ./start.sh

# 查看服务状态
docker compose ps

# 查看日志
docker compose logs -f prometheus
docker compose logs -f grafana

# 重启服务
docker compose restart

# 停止服务
docker compose down

# 清理数据（谨慎！）
docker compose down -v

# 测试监控系统
./test.sh

# 查看 Comet 指标
curl http://localhost:9091/metrics

# 查看 Prometheus Targets
curl http://localhost:9090/api/v1/targets


访问地址:
═══════════════════════════════════════════════════════════════════════

🌐 Grafana:        http://localhost:3000  (admin/admin123)
📊 Prometheus:     http://localhost:9090
🔔 Alertmanager:   http://localhost:9093
📜 Loki:           http://localhost:3100

📈 Comet Metrics:  http://localhost:9091/metrics
📈 Logic Metrics:  http://localhost:9092/metrics  (待集成)
📈 Job Metrics:    http://localhost:9093/metrics  (待集成)
```
